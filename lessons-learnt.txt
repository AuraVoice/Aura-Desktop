LESSONS LEARNT
==============

Purpose: Keep only durable engineering rules learned from real failures. Each lesson must remain
short enough to scan, must describe a reusable failure class, and must not become a chronological
debug diary. Unresolved audit findings are labeled "Required fix" instead of "Solution".

Date: 2026-07-03
Problem: The Tauri window became "Not Responding" during screen capture.
Issue: A synchronous Tauri command performed DXGI capture, image conversion, JPEG encoding, and
base64 work on the native message-loop thread. Windows could not pump window messages while that
work ran.
Solution: Make commands async by default. Put CPU-bound or blocking work in
tauri::async_runtime::spawn_blocking, and avoid encoding or serialization round trips when raw IPC
bytes are sufficient.
Justification: A non-async Tauri command shares the thread that keeps the native window alive.
Compilation cannot detect UI-thread starvation, so command threading is an architectural rule.

Date: 2026-07-04
Problem: Desktop voice repeatedly timed out even though the agent had joined and published audio.
Issue: The client waited for custom data messages that the backend never emitted. It also armed the
agent watchdog before token fetch and room connection, and initially missed agents already present
when Room.connect completed.
Solution: Verify wire contracts against the real producer and a working reference client. Use
LiveKit participant kind, participant attributes, transcription, and audio-track events. Scan
remoteParticipants immediately after connect, and arm join timeouts only after connect succeeds.
Route every terminal error through one durable logger with the room ID and error code.
Justification: Timeout tuning cannot repair an unreachable success condition. Observability and
protocol verification must precede timing changes.

Date: 2026-07-04
Problem: Overlay transitions could deadlock, repeat side effects, or leave the fullscreen pointing
window permanently click-through.
Issue: Overlay state locks were held across native window calls that synchronously re-entered event
handlers. Some direct state transitions did not emit the matching React event, and applied-state
caches could be updated inconsistently with the side effect they represented.
Solution: Hold state locks only for short reads or writes, never across window APIs. Every Rust
presentation transition must emit the event React uses to render it. Normal presentations must
restore cursor interaction defensively. Write applied-state caches only after resize, position,
show, or hide succeeds.
Justification: Native window APIs are re-entrant. State, native geometry, and React rendering must
either converge together or remain retryable after failure.

Date: 2026-07-04
Problem: The 3D avatar silently hung, framed the wrong pose, and swallowed clicks inside the drag
region.
Issue: Vite development pre-bundling broke DRACO's inferred decoder path, bind-pose bounds differed
from the animated pose, and a div with role=button was still treated as a window drag target.
Solution: Import the exact DRACO decoder assets and set their URLs explicitly. Evaluate the first
animation frame before measuring bounds. Use real button elements for interactive controls. Build
a small browser harness with visible bounds and frustum helpers before changing camera constants.
Justification: Build success does not validate the development loader, and correct camera math is
still wrong when it measures geometry that is not actually rendered.

Date: 2026-07-04
Problem: Sign-out could stall or allow an old async completion to reassert stale window state.
Issue: Cleanup awaited slow network-backed operations, and work started before sign-out could
finish after the account or foreground generation had changed.
Solution: Keep local sign-out bounded and independent of best-effort remote cleanup. Stamp async
work with an account or generation identifier and verify it again before applying completion state.
Justification: Cancellation alone is not a correctness mechanism. Stale completions must be made
unable to mutate the new session.

Date: 2026-07-05
Problem: Features existed in source but were unreachable from the states users actually entered.
Issue: Google sign-in and feedback were implemented without checking every real navigation entry
point, including returning-user screens that skipped onboarding.
Solution: Treat reachability as part of implementation. For every feature, trace at least one path
from each persisted landing state, not only the fresh-install path. Add a direct entry point or an
explicit reason the state cannot reach the feature.
Justification: Compiled code with no reachable control is equivalent to an unshipped feature.

Date: 2026-07-05
Problem: A cross-repository Google sign-in flow was correct on disk but failed for every live user.
Issue: Desktop, backend, and web deploy differently. The backend Docker build can include untracked
files, while the web deployment only receives committed and pushed files. Local source inspection
therefore gave a false picture of the deployed contract.
Solution: For cross-repository flows, verify every live endpoint and deployed artifact after an
approved deploy. Check route existence, request and response shapes, and the exact browser or app
handoff. Never equate "present locally" with "deployed".
Justification: A distributed feature is only as live as its least-deployed participant.

Date: 2026-07-05
Problem: A hardened CSP passed type-checking but broke Tauri IPC and runtime network behavior.
Issue: connect-src omitted runtime transport schemes such as ipc/http://ipc.localhost and was
derived from source-level URLs rather than the protocols the packaged webview actually used.
Solution: Build CSP allowlists from the packaged runtime, Tauri internals, Firebase, telemetry, and
backend transports. Verify IPC, authentication, voice, analytics, and error reporting in the real
webview after any CSP change.
Justification: CSP failures are runtime integration failures. Static checks cannot prove that a
webview can reach its native bridge or vendors.

Date: 2026-07-05
Problem: Installing Sentry risked replacing its panic hook, and development errors polluted the
same issue feed as installed-user failures.
Issue: std::panic::set_hook replaces the prior hook, and development sessions lacked a reliable
environment boundary in telemetry.
Solution: Chain panic hooks using take_hook before installing application logging. Disable remote
reporting in development and tag release events with environment and version.
Justification: Error reporting must preserve upstream crash capture, and an unresolved feed is only
useful when it represents real deployed users.

Date: 2026-07-08
Problem: A feature appeared absent because the installed release handled the launch instead of the
new development binary.
Issue: Dev and release shared the same Tauri single-instance identity. The old tray process accepted
the launch and displayed old bundled assets while the new dev process exited.
Solution: Keep single-instance behavior release-only. When behavior contradicts local source,
confirm the running artifact from asset origin, bundle hash, version, process path, and durable
logs before debugging feature code.
Justification: Testing the wrong binary produces convincing but meaningless evidence.

Date: 2026-07-08
Problem: A global hotkey collision crashed the entire tray application at startup.
Issue: Shortcut registration used ? in setup, so another process owning one key combination turned
a convenience failure into a process-killing setup error.
Solution: Register each shortcut independently, log and report failures, and continue booting with
tray access available.
Justification: Fail fast for corrupted invariants, not for optional operating-system integrations.
A missing shortcut should reduce convenience, not availability.

Date: 2026-07-08
Problem: Feedback log attachment silently failed because code hardcoded a log filename that did not
match tauri-plugin-log's generated filename.
Issue: Two call sites attempted to duplicate a value already derived from package metadata.
Solution: Derive generated paths and names from the same package or plugin source of truth. When a
file cannot be found, inspect the real directory instead of trusting comments or guessed names.
Justification: Replacing a wrong hardcode with a currently correct hardcode only delays recurrence.

Date: 2026-07-09
Problem: Adding more below-bar cards multiplied Rust booleans, applied caches, and duplicated
priority rules, causing redundant resize and focus side effects.
Issue: The same "which surface wins" decision existed in Rust geometry, Rust cache updates, and
React rendering. One applied cache was not updated on the normal success path.
Solution: React owns surface priority and sends one slot_height to Rust. Rust owns only native
geometry, with one applied_slot_height written after success.
Justification: Represent mutually exclusive UI with one discriminant or dimension. Parallel
booleans and mirrored priority logic create invalid combinations and forgotten cache updates.

Date: 2026-07-09
Problem: Subscription webhooks could permanently miss payments, restore revoked access, or ignore
chargebacks. This is an unresolved audit finding.
Issue: The event claim and entitlement write are separate Firestore operations. State is chosen
from event names even though Dodo retries can arrive out of order. Every event also assumes
data.metadata.firebase_uid, while dispute payloads identify only the payment.
Required fix: In one Firestore transaction, resolve a durable payment/subscription/customer to UID
mapping, reject stale transitions using authoritative payload state and event time, write the
entitlement, and create the billing event record. Test real Dodo fixtures for every event family.
Justification: Signature verification and duplicate detection are necessary but insufficient.
Billing correctness also requires atomicity, ordering defense, and resource-specific identity.

Date: 2026-07-09
Problem: A user can receive Pro without a durable entitlement or create multiple paid subscriptions.
This is an unresolved audit finding.
Issue: Missing entitlement documents resolve to Pro in enforcement paths, while trial stamping
depends on a cooperative client calling GET /entitlement. POST /billing/checkout does not reject or
reuse an existing paid billing relationship.
Required fix: All protected server entry points must atomically get or create the entitlement.
Checkout must enforce one billing relationship per UID, reuse dodo_customer_id, and be idempotent
across retries and devices.
Justification: Clients cannot be trusted to call initialization endpoints in order or to prevent
duplicate charges. Entitlement and billing uniqueness are server invariants.

Date: 2026-07-09
Problem: Desktop can serve one account's cached paid entitlement to the next signed-in account.
This is an unresolved audit finding.
Issue: The Tauri store uses one global cache key with no Firebase UID. Sign-out clears React state
but leaves the persistent cache, and a fresh cache suppresses the next backend fetch.
Required fix: Store the UID with the cached payload or key entries by UID. Require the current UID
when reading and writing, reject mismatches, and test paid-to-free account switching.
Justification: Every user-derived cache is part of the authorization boundary. TTL validation does
not replace identity validation.

Date: 2026-07-09
Problem: Trial and subscription lifecycle fields can disagree, and the launch migration can fail to
grant the promised trial. This is an unresolved audit finding.
Issue: Stored trialing status is trusted after trial_end_date, paid status is not reconciled with
expires_at, and the migration scans only existing entitlement docs while updating dates without
resetting eligible accounts to trialing.
Required fix: Resolve status and effective tier from authoritative dates and billing state. Migrate
from the users collection, upsert missing entitlement docs, reset eligible free users to trialing,
and test expired, missing, paid, cancelled, and concurrent cases in dry-run and apply modes.
Justification: A migration that updates dates but leaves a status that overrides them has changed
data without changing access.

Date: 2026-07-09
Problem: Mobile steering and checkout-return behavior can show prohibited or false purchase UI.
This is an unresolved audit finding.
Issue: Mobile treats locale country as store storefront. The web return page claims payment success
for both success and failure redirects, and targets an aura:// link that no current client handles.
Required fix: Use an authoritative storefront signal or default to SILENT. Render checkout state
honestly, verify entitlement before claiming unlock, and register and test a dedicated return deep
link before attempting it automatically.
Justification: Billing UI is a policy and trust boundary. Locale, redirect arrival, and an
unregistered URL scheme are not proof of storefront eligibility, payment, or app availability.

Date: 2026-07-09
Problem: The 2026-07-09 subscription audit findings above (13 total across the four repos) were
open: webhook uid resolution, event ordering, claim/merge atomicity, duplicate subscriptions,
missing-doc-grants-Pro, date-blind status, migration gaps, the desktop cache leak, locale
steering, the dishonest checkout return page, copy mismatches, ignored background pushes.
Issue: Resolved in this pass. Backend: reverse dodo_customers/dodo_subscriptions/dodo_payments
mappings + payment.succeeded and subscription.updated handling, one Firestore transaction for
claim + staleness + mappings + entitlement merge (last_billing_event_at guard), checkout 409 +
customer reuse, ensure_entitlement_doc get-or-create in every enforcement path, normalize_status
with a 3-day renewal grace, reset_trials rewritten to iterate users and reset status. Desktop:
entitlement cache keyed by uid, cleared on sign-out (clear_entitlement_cache). Web: return page
renders success/pending/failure from Dodo params and auto-fires aura://checkout only on success.
Mobile: steering from the backend-resolved country (silent when unknown), aura://checkout
registered on Android + iOS, background push persists a SharedPreferences refresh marker consumed
on resume, paywall copy matches enforced limits.
Non-obvious constraints hit along the way: a terminal webhook (dispute/refund) with no resolvable
uid must 500 for redelivery, never 200, because its payment mapping may land seconds later from a
payment.succeeded still in flight; a fake Firestore transaction must BUFFER writes and flush only
on success or atomicity tests pass vacuously; flutter test runs ENV=dev so SubscriptionService's
dev bypass shortcuts refreshEntitlement (tests need a @visibleForTesting state hook); the Flutter
repo's generated mockito mocks were stale after NotificationService grew entitlementUpdatedStream,
failing 23 unrelated tests until build_runner regenerated them.
Justification: Closes the audit trail; the constraints above are the ones a future pass would
otherwise re-discover the hard way.

Date: 2026-07-10
Problem: On a live call, Buddy dictated a PowerShell command aloud, symbol by symbol, when asked
to "draft a command", and generally lectured (long unbroken monologues) and answered profanity
with a therapist script ("I feel your frustration...").
Issue: All three were prompt/tool-surface gaps, not model bugs. The draft tool only modeled text
SENT to a person (email_reply | cold_dm), so copy-exact text (commands, code, config) had no
render path and speech was the model's only output; the brevity rule's "unless they ask for
detail" escape hatch was unbounded, and any technical question reads as asking for detail; the
persona prompt had zero guidance for user aggression, so the model fell back to its default
de-escalation register.
Solution: New "snippet" channel through the existing draft pipeline (frame optional, balanced
tier when frameless, no length ladder, quota-exempt, monospace card render with a
Regenerate-only chip row), a hard four-sentence ceiling even in detail mode plus an explicit
"put copyable answers on screen" pressure valve, and a "When they snap at you" prompt section
with the de-escalation phrases banned verbatim.
Non-obvious constraint: adding a value to a data-channel enum is a cross-repo contract change
with a forced release order. An old desktop client receiving channel "snippet" drops the
draft.created payload in parseCreated and the card sits on the loading skeleton forever, so the
desktop release that understands the new value must ship BEFORE the backend that emits it.
Justification: A model can only follow the output paths it's given; when the only path is
speech, every gap becomes dictation. Bound the escape hatches and give long-form output a
non-speech surface instead of prompt-scolding the model harder.

Date: 2026-07-11
Problem: Building Meeting Notes capture (MEETING_NOTES_PLAN.md Phase 1), two audio-pipeline
assumptions from the design doc turned out wrong before a line of capture code ran, and one
process assumption about the plan doc itself.
Issue: (1) The plan budgeted a resampler dependency (rubato) and a format-conversion matrix for
WASAPI's device formats. Unnecessary: shared-mode WASAPI with AUDCLNT_STREAMFLAGS_AUTOCONVERTPCM
(wasapi crate: StreamMode::PollingShared { autoconvert: true }) accepts a requested 16 kHz mono
f32 format directly and the Windows audio engine does the conversion, for both the mic and the
render-loopback stream. (2) WASAPI loopback delivers packets ONLY while something is actively
rendering; render-silence means no packets at all, not zero-filled packets, and event-driven
timing goes quiet with it. A loopback recorder that just appends what arrives compresses silent
spans out of the timeline and desyncs the two channels. (3) The plan doc's own "blocked on
backend entitlement plumbing" prerequisite was stale: the /entitlement and /billing/* routes it
declared unbuilt were live in production, verifiable in one minute with unauthenticated probes
(401/405 = deployed and gated, 404 = absent).
Solution: No resampler crate at all, request the target format and let autoconvert do it; poll
both streams and wall-clock silence-fill each one past a 200ms guard so channel alignment and
segment duration stay honest through render-silence; and before treating any plan document's
"current state" claims as true, probe the live endpoints (the 2026-07-05 deploy-state lesson
applies to design docs, not just code reviews).
Justification: Each of these would have surfaced as a confusing runtime symptom (format
mismatches on exotic devices, transcripts with vanished silent minutes, a feature gated on a
prerequisite that already shipped) instead of a design-time decision.

Date: 2026-07-11
Problem: A staff-engineer-mode external review (codex) of the freshly built Meeting Notes
feature surfaced 15 findings the build session's own checks (compile, types, unit tests) never
touched, four of them critical.
Issue: The failure classes were systemic, not typos. (1) Client-supplied metadata that feeds a
server-side spend cap (segment start_ms/duration_ms driving the STT budget) was range-unchecked
and trusted by the worker, so a modified client could bypass the free-tier cap entirely.
(2) A status compare-and-set that allows X->X re-entry (synthesizing->synthesizing, meant for
crashed-worker recovery) is NOT idempotency under at-least-once delivery: a concurrent duplicate
also passes and double-runs the whole paid pipeline; recovery needs a lease with an expiry, not
a permissive transition. (3) Multiple threads doing load-modify-save on one JSON manifest with
no shared lock silently drop whichever mutation saves first. (4) Recording consent stored
without a uid namespace is granted by the INSTALL, not the person: the next account on the same
Windows profile inherits the opt-in, and sign-out didn't stop a live capture. (5) A privacy
filter (sensitive-meeting exclude list) that fails open on a read error ships exactly the data
it exists to protect to a third-party vendor on a transient blip.
Solution: All fixed same-day: range checks + cumulative-duration cap + segment-count ceiling,
a transactional synthesis lease (fresh lease refuses duplicates, stale lease reclaims), a
process-wide manifest mutex plus an ack-refused-while-capturing guard, uid-scoped arm keys plus
stop-capture-on-sign-out, and exclude-list reads that raise (retry with audio intact) instead
of defaulting to empty. Plus: terminal-vs-retryable classification for vendor rejections (a
non-429 4xx retried forever is a retry storm), and capture persistence failures now stop the
recording instead of letting the indicator claim health while audio is lost.
Justification: Every one of these passes compilation, type checks, and happy-path unit tests.
The reviewable properties (cap integrity under a hostile client, at-least-once semantics,
cross-thread file access, consent scoping, fail-open vs fail-closed on privacy filters) need an
adversarial pass with exactly those lenses; budget one for any feature that touches money,
consent, or third-party data flows before it ships.

Date: 2026-07-13
Problem: Voice answers containing PowerShell commands, code, prompts, or next steps were spoken
aloud even after users repeatedly asked for a draft, because the only visible-output path was
modeled as an outbound email/DM draft.
Issue: Output modality is a capability, not a message channel. A narrow pre-inference classifier
removed the only useful display tool, so the model could not recover. Blanket screenshot and
persistence assumptions also made a command unnecessarily depend on screen sight and risked
mixing reusable artifacts into the personal draft feed. Overlay state could hide a valid event
while pointer mode was cleaning up.
Solution: Add a deterministic, no-frame, no-write visible-artifact capability and a focused
per-tool skill injected only when that tool is exposed. Keep `draft.created` plus `snippet` as the
compatibility envelope, add optional Markdown/code metadata, keep presentation artifacts
ephemeral, and summon the overlay on arrival. Remember only whether the prior requested artifact
succeeded so "again" can repair a failed turn without storing content. Test negative frustration
phrases, stale turns, missing fields, packet limits, publish failures, old payloads, and overlay
arrival states.
Justification: A perfect prompt cannot call a tool that deterministic orchestration removed.
Capability routing, execution safety, transport truthfulness, rendering, and recovery therefore
need separate contracts and tests. Tool-specific skills improve accuracy without bloating every
voice turn, while policy code remains the authority for side effects and freshness. 

Date: 2026-07-14
Problem: Ctrl+Alt+B appeared unable to hide the overlay after a draft or visible artifact arrived.
Issue: The native hotkey did hide the window successfully, but useDraftCard treated every later
Hidden presentation as a failed artifact presentation and immediately invoked summon. Logs showed
the window completing Hidden and returning to Panel 20 to 25 ms later. The recovery effect was
intended only for an artifact that arrived while the full-screen pointer animation was in flight,
but it did not remember whether pointer mode was actually the reason visibility had been deferred.
Solution: Track a pending restore only when a draft event arrives during Pointing. When Pointing
ends, consume that one-shot marker and restore only if the prior presentation returns Hidden.
Existing drafts no longer reopen the overlay after a user hotkey or Esc hide. A pure regression
test pins intentional hide, pointer wait, hidden restore, and visible-panel cleanup behavior.
Justification: Recovery state must record the specific interrupted transition it is recovering
from. Deriving recovery from "content exists and the window is hidden" overrides an explicit user
action and turns a successful native hide into a visible no-op.
(V0.2.3 makes Buddy’s useful text easier to see and reuse. PowerShell commands, code, configuration, prompts, checklists, and next steps now open in a copyable on-screen card instead of being read aloud, with Markdown formatting and exact whitespace preservation for runnable content. Buddy can also recover when asked to show something again, automatically restores the overlay when needed, and keeps email and message drafts separate from temporary visible artifacts.)

Date: 2026-07-14
Problem: A real 22-minute meeting capture produced no summary or insights, with no visible error.
Issue: The meeting-audio bucket gs://juno-2ea45-meeting-audio was never provisioned, so every
segment upload got GCS 404 "bucket does not exist", which the handler collapsed to HTTP 503. deploy.sh
only DOCUMENTED the bucket as a manual one-time step in a comment and never verified it, so a revision
that could not accept any meeting audio shipped and took 100% of traffic silently. The client behaved
correctly: the desktop's durable encrypted queue retained all five segments and kept retrying.
Solution: Added scripts/check_meeting_storage.py (backend) as a read-only preflight for bucket
existence, region, and the 7-day lifecycle rule, and wired it into deploy.sh BEFORE gcloud run deploy
so a missing or misconfigured bucket aborts the deploy under set -euo pipefail. Runtime uploads stay
retryable. Recovery of the stuck meeting needs no client change: restart resets the in-memory backoff
and the queue re-uploads.
Justification: A provisioning prerequisite that lives only in a comment is not a prerequisite. An
infrastructure dependency the app hard-requires must fail the deploy that lacks it, not fail silently
at runtime as a routine-looking 503. Distinguish a config failure (bucket missing) from a transient
provider error so the first is caught before traffic shifts and the second stays retryable.

Date: 2026-07-15
Problem: The notification inbox's "Retry upload" button could close the inbox and do nothing, with
no feedback, so the user believed they had retried when they had not.
Issue: A retry action and its retryable local activity live in two separate durable stores. After a
restart that purged the native segment queue, the pump reconcile flips the activity to
failed/upload_expired (retryable: false) while the notification row, in its own store, still shows
the Retry button. retryNow() bailed silently when no retryable activity remained, but the caller
closed the inbox and returned regardless, producing a dead action.
Solution: retryNow() now returns whether it actually started a retry. The caller closes the inbox
only on true; otherwise it falls through to the existing openDashboard() default so the click always
lands the user somewhere the real meeting state is visible.
Justification: An action button whose backing state can be absent must report success, not fail
open. When two stores can disagree about whether an action is still possible, the UI must degrade to
a meaningful fallback instead of a silent no-op that reads as "done."

Date: 2026-07-15
Problem: A voice session could be toggled off while token fetch or LiveKit connection was still
pending, then continue forward and enable the microphone after the user had already ended it.
Issue: Disconnecting the room reference is not cancellation for an async start pipeline. Each
resolved await resumes its original closure unless the user's latest intent is checked again.
Solution: Keep synchronous desired-active state plus a monotonically increasing session generation.
Every token, connect, and microphone await verifies both before advancing. Ending increments the
generation, closes native voice authorization immediately, disables the microphone, and disconnects
any partial room. Native Right Ctrl events carry a separate sequence used only for UI deduplication.
Justification: User intent must be the authority across every asynchronous boundary. Resource cleanup
alone cannot prevent stale code from creating or re-enabling the resource after cleanup ran.

Date: 2026-07-16
Problem: A global single Ctrl tap was too easy to trigger accidentally once the resting companion
was removed and voice became summon-on-demand.
Issue: The low-level hook correctly identified an isolated Right Ctrl release, but putting timing in
that Windows callback would add avoidable work to a latency-sensitive system hook.
Solution: Keep the hook and its chord-cancelling TapState unchanged. Classify tap timing in the async
receiver instead, emit aura-toggle only for two isolated taps within 250 ms, and drop lone taps.
Justification: System hooks should do the minimum possible work. A pure timestamp classifier keeps
the interaction testable without making the keyboard callback allocate, wait, or emit UI events.

Date: 2026-07-16
Problem: The notch and draft share one native window, but adding draft height could move the notch
away from the fixed top-center anchor.
Issue: Companion geometry treats the persisted center as the owl base and grows a surface upward.
Applying that rule to the notch would shift the bar whenever the draft slot opened.
Solution: Give Bar its own fixed top-center position that ignores slot height and persisted center.
Its window height grows downward from 72 px while the Companion path keeps its upward-growth math.
Justification: Shared state does not imply shared anchoring. Each presentation needs geometry based
on the visual element that must remain stationary during a slot resize.

Date: 2026-07-16
Problem: The voice gesture depended on Right Ctrl, which compact keyboards may omit entirely.
Issue: The Windows virtual-key constant and its label were hardcoded in several native paths, so
changing the key risked leaving installation logs and status payloads inconsistent with the hook.
Solution: Select the physical key through one VOICE_TOGGLE_KEY constant with LeftCtrl, RightCtrl,
and EitherCtrl choices. The hook matcher, native status label, and readiness log derive from it.
Left Ctrl is selected because it is present on standard and compact Windows keyboards.
Justification: A global gesture must use hardware the target audience actually has. Centralizing the
choice also turns future key experiments into one reviewed value change instead of a search-and-edit.

Date: 2026-07-16
Problem: A second voice session could connect to an already-present agent, look healthy, then report
agent_silent and retry even though its audio track had already subscribed.
Issue: LiveKit can emit TrackSubscribed before connect resolves. That event cleared the initial
silence watchdog, but the later already-present-agent scan armed it again. With no later agent event,
the re-armed timer produced a false error. Separately, the 250 ms Ctrl pair window was too strict for
normal human tapping and rejected pairs were invisible in logs.
Solution: Skip the initial agent silence watchdog when assistant output was already observed, cover
both LiveKit event orders with a pure regression test, allow 400 ms between Ctrl taps, and trace tap
intervals plus native-to-webview action timing in development logs.
Justification: Event handlers must be correct in every valid delivery order. A summon gesture should
also fit ordinary motor timing and explain rejected attempts without recording unrelated key data.

Date: 2026-07-16
Problem: Double-tapping Left Ctrl could summon the notch, then fail to dismiss it until the user
clicked another window and tried again.
Issue: Summoning forced our overlay to the foreground, and win_focus injects a lone Alt tap to win
SetForegroundWindow's "last input event" exception. A lone Alt press-release drops the newly
foreground window into Windows keyboard menu mode, which then swallows the following Left Ctrl
double-tap entirely (no hook emit at all while our window held focus). Clicking another window exits
menu mode, which is exactly why the workaround worked. An earlier fix that only filtered
LLKHF_INJECTED events out of TapState was necessary but not sufficient: it stopped the injected Alt
being miscounted as a chord, but never addressed that same Alt trapping our own window in menu mode.
The tell was in the logs: every dismiss that emitted a toggle had happened after focus already left
our overlay; the failed ones left no trace because the hook produced nothing.
Solution: Do not force foreground for the passive, hotkey-controlled notch/Bar at all (apply, summon,
summon_bar). Reserve force_foreground for the Setup panel, whose sign-in fields genuinely need
keyboard focus. Always-on-top keeps the notch visible without stealing focus, so no Alt is injected on
the notch path and the global hotkey stays reliable. The LLKHF_INJECTED filter stays for the panel
path. Tradeoff: the notch no longer holds focus, so Escape-to-dismiss needs a global hotkey if wanted.
Justification: A passive overlay driven by a global hotkey must not steal foreground focus. The very
act of injecting synthetic input to win that focus also injects the side effect (menu mode) that
disables the hotkey, so filtering the symptom could never be enough. Removing the focus grab removes
both the injection and the focus-dependent failure. When a bug is focus-dependent, suspect what
grabbing focus injects, not only what the injected event does downstream.

Date: 2026-07-16
Problem: A visual-root rewrite left meeting recording services unmounted, retained plaintext turn
screenshots, and allowed late asynchronous voice and pointer work to cross user-visible boundaries.
Issue: Background services were coupled to removed UI composition. Turn capture mixed an ephemeral
LiveKit payload with a durable JPEG side effect. Native notch presentation could not return failure,
and room-independent fallback geometry let an unknown frame id reuse a prior capture.
Solution: Mount consent and meeting-capture hooks independently of visual controls, keep turn frames
memory-only and purge the legacy screenshot directory, await successful notch presentation before
voice startup, invalidate token/connect/microphone work on stop, and scope exact frame ids to one room.
Justification: Removing a surface must not remove its approved background service. Sensitive capture
must begin only after visible state is confirmed, and ephemeral/session-scoped data must not outlive
the storage or session boundary promised to the user.

Date: 2026-07-16
Problem: A v0.3.0 release displayed v0.2.4 and shipped v0.2.4 assets, while a fresh signed-out
manual launch left the setup and sign-in UI hidden in the tray.
Issue: The v0.3.0 Git tag pointed to the v0.2.4 commit, so the release action correctly built the
old manifests under the new tag. Separately, the notch rewrite removed the startup summon block
instead of only changing the signed-in presentation, so manual and Windows autostart launches both
became quiet tray launches.
Solution: Reject release jobs unless the pushed tag matches package.json, tauri.conf.json, and
Cargo.toml. Restore startup summon for direct and explicit launches while keeping --autostart quiet;
an update restart still summons. Pin all three startup cases with pure tests.
Justification: A release must fail before publishing when its immutable tag disagrees with the
binary version. A user-started desktop app must expose setup when no session exists, while a boot
helper should remain unobtrusive. These are different launch intents and need separate behavior.

Date: 2026-07-17
Problem: Building a first-run onboarding tail (hotkey tour + live agent demo) that runs AFTER
sign-in, plus a native in-app dashboard window, hit two non-obvious platform constraints.
Issue: (1) OnboardingFlow only mounts while signed out (OverlayRoot branches on `user`), but the
live "talk to Buddy" demo needs a signed-in LiveKit token (authFetch throws AuthRequiredError with
no currentUser), so the demo cannot precede sign-in and cannot live inside OnboardingFlow. Worse,
AuthProvider's set_panel_variant("companion") hides the window on sign-in (overlay.rs), so a naive
post-sign-in step renders into a hidden window. (2) A second Tauri window created via
WebviewWindowBuilder loads the same dist bundle but has NO IPC/store/http access unless its label is
added to a capability's `windows` list - every invoke/fetch fails silently otherwise.
Solution: Added an OnboardingTail component rendered by a new OverlayRoot branch (user set, not-seen,
role answered), shown in a re-revealed Panel presentation via a new summon_onboarding_panel command
(with an overlay-changed listener to defeat a late hide race), and running the demo inline through the
hoisted useVoiceBar instance (never a second one - that opens a second room) without summon_bar. The
double-tap-Ctrl gesture is suppressed while the tail is active so the demo stays in the panel. The
dashboard is a runtime WebviewWindowBuilder window (label "dashboard", decorated/opaque/taskbar), with
"dashboard" added to capabilities/default.json windows, routed in main.tsx by getCurrentWebviewWindow
().label, using its own read-only auth subscription (NOT the overlay AuthProvider, whose side effects
must not run from that window). onboarding_seen now writes at the end of the tail, not at sign-in, so
an interrupted first-run resumes. Attribution answers persist pre-sign-in under a per-install
desktop_anon_id (never the literal "anonymous", which would chain-merge every install into one PostHog
person) and alias to the uid on sign-in; backend /devices/profile write is fail-soft (endpoint TBD in
juno-backend).
Justification: The demo's auth requirement forces sign-in before it, which forces the tour+demo out of
the signed-out OnboardingFlow and into a presentation that survives the auth transition. A second
window is inert without an explicit capability grant, and sharing the overlay's AuthProvider would let
a background window drive the foreground overlay's window state.

---
Date: 2026-07-17
Problem: The desktop dashboard's Conversations, Drafts, and Saved pages rendered empty for a
signed-in user who clearly had that data on auravoiceapp.com/dashboard (the production web dashboard).
Issue: This was NOT an auth, transport, or UI-consumption bug. The desktop client was pointed at the
wrong endpoints. dashboardApi.ts called GET /desktop/conversations (juno-backend filters that
projection to sessions where surface == "desktop", so mobile/web history never matches) and GET
/desktop/saved (reads the memory_atoms collection, not the user's screen_saves bookmarks); Drafts made
no call at all (hardcoded []). The /desktop/* endpoints were an aspirational desktop-only contract that
excludes most real data. Meanwhile the live web dashboard reads the full, cross-surface endpoints:
GET /history/sessions?since=<ISO> (+ /history/sessions/{id} for the transcript), GET /drafts,
GET /screen-saves - all authenticated with the same Firebase Bearer token the desktop already sends.
Solution: Repointed the desktop client at those same production endpoints (reusing the exact wire
shapes from Aura-Web src/lib/activity.ts, verified against the juno-backend handlers). Zero backend
changes. Added a two-tier (memory-over-disk) stale-while-revalidate hook (useDashboardResource) with a
freshness gate, single-flight de-dup, a hard 15s timeout so a request can never hang, an out-of-order
seq guard, and change-detected disk writes (dashboardCache.ts, plugin-store, versioned+bounded). Saved
items strip the ephemeral signed image_url before caching and render it only from a live fetch. UI is a
data-driven fixed card shell (DashboardCard populated by a CardModel prop) + windowed CardGrid +
skeletons + a portal DetailModal (scrim/Esc/X close) + range chips.
Justification: When a cross-repo data flow "exists on disk" but shows empty, verify which endpoint the
WORKING client actually calls before assuming the feature is unbuilt - the desktop-only projection
looked plausible and returned valid empty 200s, which reads as "no data" rather than "wrong surface."
The reused endpoints are the same ones production already trusts, so parity is guaranteed and no new
backend surface (or its filtering pitfalls) was introduced.

---
Date: 2026-07-17
Problem: Saved-page screen-save thumbnails rendered blank offline, on stale cache, or once a signed
URL expired (three empty cards in a real screenshot). The cache had deliberately stripped image_url.
Issue: image_url is a short-lived signed GCS URL, so caching the URL is useless (dead on read-back).
Caching the bytes in JS is also a dead end: an <img src=signedUrl> renders cross-origin fine, but
reading the bytes (fetch().arrayBuffer(), canvas.toBlob()) is blocked because GCS signed URLs send no
Access-Control-Allow-Origin. So there was no webview-side path to persist the pixels at all.
Solution: Pull the bytes through Rust (saved_images.rs: reqwest GET, no browser CORS), encrypt them at
rest with the SAME AES-256-GCM + DPAPI key the capture pipeline already uses (crate::meeting::crypto),
and hand decrypted bytes back over the binary IPC pattern meeting::read_segment established (Response
::new -> Uint8Array -> Blob URL). Render prefers the local blob over the signed URL, so a cached item
never depends on a fresh URL again. Retention self-bounds by mirroring the saved set (prune_saved_images
evicts any id no longer saved); filenames are hex(item_id) to neutralize path traversal. stripImageUrls
stays - the ephemeral URL still never touches the JSON cache.
Justification: "Cache the image" splits into two non-obvious walls: the URL is ephemeral (cache bytes,
not the URL) and the browser cannot read cross-origin signed-URL bytes (do the download in Rust, not
the webview). For private captures, reuse the existing at-rest encryption rather than dropping plaintext
on disk. Reqwest was pinned to 0.13 (not 0.12) to unify with the version sentry already pulls, avoiding
a duplicate reqwest in the tree; note reqwest 0.13 renamed the rustls-tls feature to rustls.

---
Date: 2026-07-18
Problem: Memories and proactive notifications never appeared on desktop even though the callback card
(useCallbackCard + CallbackCard.tsx) and the whole notification stack (useDesktopNotifications broker,
NotificationInboxCard, outbox poller, tray badge command) were fully built and unit-tested.
Issue: None of it was ever mounted. OverlayRoot.tsx only wired the draft card into the below-bar slot;
nothing called useCallbackCard/useDesktopNotifications, nothing invoked set_tray_unread, and nothing
listened for the tray's open-notifications-requested event, so the tray menu item summoned the bar and
then silently did nothing. A second, subtler mismatch: useCallbackCard predates the companion->notch
rename and keys its trigger off presentation === "companion", while today's signed-in notch reports
"bar", so even a naive mount would never have fired.
Solution: Phase A of PROACTIVE_FOLLOWUP_NOTIFICATIONS_PLAN.md. Mounted both surfaces into OverlayRoot's
slot resolver (priority: draft > inbox > daily catch-up, per the CLAUDE.md slot order with the inbox at
the kebab-menu tier since kebab/tray open it), mapped "bar" to "companion" at the useCallbackCard call
site instead of rewriting the tested hook, drove the single set_slot_height with per-surface height
constants that agree with each card's CSS (270 / 300 / 180), invoked set_tray_unread on unread-count
changes, listened for open-notifications-requested to fill the slot, and reset callback + inbox state on
sign-out. Added the two new card classes to the .notch-column grid-row: 2 rule. Zero Rust changes: the
slot machinery (slot_showing, size_for) already handled Bar.
Justification: "Built and tested" is not "shipped" - a feature whose render-tree mount is missing fails
with no error anywhere, and every layer below it (broker, outbox, contract validation) looks healthy in
tests. When a surface never appears, check whether its hook/component is reachable from the root before
debugging the data path. And when a hook's input union no longer matches current naming, adapt at the
call site rather than editing a tested hook in a wiring-only pass.

---
Date: 2026-07-19
Problem: A Windows toast click could be routed twice, lost during dashboard startup, or overwrite a
different unclaimed click, and an inbox refresh started before sign-out could publish stale rows later.
Issue: The native click handler both parked one `Option` payload and emitted it live, while the dashboard
listener repeatedly remounted and never atomically claimed the parked copy. Inferring listener readiness
from window existence also fails while the webview is loading. Separately, dashboard refresh promises had
no account-generation guard, so completion order could outlive the uid that initiated the read.
Solution: Keep a bounded native activation queue and make both live delivery and startup draining claim
entries through the same atomic command, with the listener installed before draining. Stabilize the React
effect on the `markSeen` callback. Tag inbox refresh/subscription work with a generation and re-check the
owner before publishing rows, invalidating outstanding generations on uid changes and cleanup.
Justification: Window existence is not listener readiness, and emitting an event is not acknowledgement.
Exactly-once handoff needs one durable queue plus an atomic claim shared by every delivery path. Async
account-scoped reads likewise need a generation or cancellation boundary before they may update UI state.

---
Date: 2026-07-20
Problem: With the notch docked left or right, the slot card (draft/inbox/callback) rendered as a ~29px
sliver stacked against the pill instead of a 380-wide landscape panel beside it. Moving the notch also
felt broken: the 450ms long press silently aborted whenever the cursor drifted off the 29px pill.
Issue: CSS grid auto-placement order. Top/bottom pinned .notch-shell with a definite grid-row, which the
grid placement algorithm resolves early, so the auto-placed card fell into the remaining track. Left and
right pinned the shell with only grid-column; column-only-locked items in a row-flow grid place in the
same late, DOM-order-driven step as auto items, and the card renders before NotchBar in OverlayRoot, so
the card grabbed row 1 / column 1 (the 29px pill track) and pushed the shell to an implicit row 2. The
Rust geometry (bar_size 415x270) was correct the whole time. The move gesture failed for an unrelated
reason: onPointerLeave cancelled the long-press timer, and any drift off a 29px-tall target during a
450ms hold leaves it.
Solution: Give BOTH the shell and the card explicit grid-area per edge so placement never depends on DOM
order or the auto-placement algorithm, and add per-edge card enter keyframes (animation-name override
only, so each card's reduced-motion animation:none still wins). Replaced the long press with an instant
drag: pointer capture on press, begin_notch_move once movement crosses 6px, plain click is a no-op.
Dimmed the edge-picker highlight to a 20% glass-accent tint (the old 42%/55% fallback teal was neon
because --accent-base was never defined anywhere).
Justification: A grid item pinned only on the axis that matches the grid's flow direction does NOT get
early placement; pin both axes (grid-area) whenever correctness depends on which cell an item lands in.
And a hold-still-then-drag gesture on a small target is self-defeating: the hold requirement and the
target size fight each other, so gate on movement, not time.

Date: 2026-07-21
Problem: Double-tap Ctrl took 20-30s to first voice response (LiveKit Cloud Agents worker cold start
on the free plan). Tempting "fix": run local whisper.cpp STT + a direct LLM call to answer before the
worker wakes, or a router that hands the live turn from a local cascade to LiveKit once it is up.
Issue: The LLM is not a standalone API call. It runs inside the agent session (backend
buddy_agent.py llm_node) together with per-turn memory retrieval, tool policy, and Cartesia TTS. A
cold worker means no brain at all, so client-local STT cannot mask the cold start. Once warm, local
whisper.cpp is also slower than today's Deepgram path, which streams interims and starts the LLM on
partial text (preemptive_generation), and it would force VAD/EOU/interruption to be rebuilt on machines
we do not control. A pipeline-to-pipeline takeover mid-turn is a seam with two brains and no shared
session state.
Solution: Fix latency where it lives: keep a warm agent (LiveKit Cloud warm pool) and pre-dispatch the
transport from the client. Split useVoiceBar startSession into prepareSession (token + room connect +
agent dispatch, no user-visible effect) and activateSession (mark call live + open mic). The notch
gesture fires prepareSession in parallel with the native summon, then activateSession only after
summon_bar resolves, so the agent is already joining while the notch appears yet the mic still never
opens before the bar is visible. If a text fast-lane is ever wanted, the sanctioned path is the
backend's existing text_input -> session.generate_reply (screen_context.py), which reuses the same
brain - but it still needs the room+agent up, so it does not help cold start.
Justification: Latency belongs to whichever stage is actually slow; a second local pipeline cannot
substitute for a component that only exists server-side. Also: overlay::set_voice_active (overlay.rs)
is pure bool bookkeeping (updater gate + screen-sight disarm on true->false), with no presentation
change and no apply() call, so deferring it into activateSession cannot race summon_bar's window show.

Date: 2026-08-01
Problem: A modifier chord containing Left Ctrl could open a cloud LiveKit voice call.
Issue: voice_toggle_key.rs treats an isolated Left Ctrl press-and-release as a voice toggle, and
TapState::observe only cancels the candidate when a partner key goes down DURING the hold. Press the
partner FIRST (Win then Ctrl for the new Ctrl+Win dictation chord) and the cancel branch never runs,
so the Ctrl keyup emits a toggle. Two of those inside DOUBLE_TAP_MS (400ms) classify as a double tap
and start a real cloud voice session. The defect is press-order dependent, which is exactly why
casual testing misses it.
Solution: Track the chord's own key states in the same low-level hook and carry a sticky "suppressed"
flag for the rest of the hold once the chord is engaged, rather than relying on the during-hold
cancel branch. Gate it on DICTATION_CHORD.suppresses_voice_toggle(), which is derived from both
constants (chord key set vs VOICE_TOGGLE_KEY) instead of hardcoding "Left Ctrl", so changing either
one keeps the two in agreement. Reset the flag when a fresh hold starts, so a keyup the hook never
saw (lock screen, fast user switch) cannot swallow the next legitimate tap.
Justification: Two features sharing one physical key is a state-machine overlap, not a key-choice
problem, and picking an exotic key would only hide it. Any cancel rule written as "a partner key goes
down while ours is held" is order dependent by construction and will keep producing this class of
bug; the durable rule is to make the suppression sticky for the whole hold and derive it from both
key constants rather than from one of them.
